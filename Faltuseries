#include<stdio.h>
int main()
{
int n,sum=0,temp=0,i,j;

printf("Enter a number:");
scanf("%d",&n);


for(i=1;i<=n;i++)
{
    for(j=1;j<=i;j++)
    {
        printf("%d + ",j);
        sum=sum+j;
    }
}
printf("Sum is:%d",sum);
    
return 0;
}
